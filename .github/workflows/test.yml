name: Continuous Integration
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    container:
      image: alpine
      options: --privileged
    steps:
      - run: |
          env | sort
      - run: |
          ls /
